#include<bits/stdc++.h>

using namespace std;
vector<int> vec;

int main()
{
	freopen("ipl.in","r",stdin);
	int N,max=0,selected=0;
	cin>>N;
	int* arr=new int[N];
	for(int i=0;i<N;i++)
		cin>>arr[i];
	for(int i=0;i<N;i++){
		if(selected)
	}
}